{
  "name": "traktgram",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "deploy": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "set-secret": "wrangler secret put"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240712.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.99.0"
  },
  "dependencies": {
    "grammy": "^1.43.0"
  },
  "engines": {
    "node": ">=20"
  }
}
